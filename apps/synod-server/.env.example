JWT_SECRET=
# Set manually, or leave empty and configure from `synod setup` / `synod dashboard`
VAULT_PATH=
PORT=3000

# Optional: public URL of this server, used for invite claim redirects
# SYNOD_SERVER_URL=https://collab.example.com

# Optional: short-lived download link TTL in minutes (default 15)
# SYNOD_BUNDLE_GRANT_TTL_MINUTES=15

# Optional: bootstrap token TTL in hours (default 24)
# SYNOD_BOOTSTRAP_TOKEN_TTL_HOURS=24

# Optional: comma-separated path deny list for bundle packaging policy
# SYNOD_BUNDLE_DENY_PATHS=.git,.synod,.synod-quarantine

# Optional: store managed-state.json outside the vault directory
# Defaults to <VAULT_PATH>/.synod/managed-state.json
# SYNOD_STATE_PATH=/var/lib/synod

# Optional: host path for the vault when using Docker Compose
# HOST_VAULT_PATH=./vault
