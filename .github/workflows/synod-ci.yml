name: synod-ci

on:
  pull_request:
    paths:
      - 'server/**'
      - 'release/synod-client.lock.json'
      - 'artifacts/synod-client/**'
      - 'tools/synod-client/**'
      - '.github/workflows/synod-ci.yml'
  push:
    branches:
      - main
    paths:
      - 'server/**'
      - 'release/synod-client.lock.json'
      - 'artifacts/synod-client/**'
      - 'tools/synod-client/**'
      - '.github/workflows/synod-ci.yml'

jobs:
  verify:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node-version: [20, 22]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm
          cache-dependency-path: server/package-lock.json

      - name: Verify pinned synod-client artifacts
        run: node tools/synod-client/verify-lock.mjs

      - name: Install synod-server dependencies
        working-directory: server
        run: npm ci --workspaces=false

      - name: Lint
        working-directory: server
        run: npm run lint

      - name: Format check
        working-directory: server
        run: npm run format:check

      - name: Run tests
        working-directory: server
        run: npm test

      - name: Build server package
        working-directory: server
        run: npm run build
