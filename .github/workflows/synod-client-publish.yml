name: synod-client-publish

on:
  push:
    tags:
      - 'synod-client-v*'
  workflow_dispatch:
    inputs:
      release_tag:
        description: 'Release tag to publish (example: synod-client-v1.2.3)'
        required: true
        type: string

permissions:
  contents: write

jobs:
  publish:
    if: ${{ github.event_name == 'workflow_dispatch' || github.actor != 'github-actions[bot]' }}
    runs-on: ubuntu-latest
    env:
      RELEASE_TAG: ${{ github.event.inputs.release_tag || github.ref_name }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ env.RELEASE_TAG }}

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install workspace dependencies
        run: npm ci

      - name: Verify local contracts sync
        run: npm run contracts:verify

      - name: Build and validate BRAT assets
        run: npm run build:brat --workspace @fyresmith/synod-client

      - name: Build release checksums
        run: npm run build:checksums --workspace @fyresmith/synod-client

      - name: Ensure tag matches package + manifest
        run: npm run --workspace @fyresmith/synod-client assert-tag-version -- "${{ env.RELEASE_TAG }}"

      - name: Create GitHub release with BRAT assets
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.RELEASE_TAG }}
          target_commitish: ${{ github.event.repository.default_branch }}
          name: 'Synod Client ${{ env.RELEASE_TAG }}'
          prerelease: ${{ contains(env.RELEASE_TAG, '-') }}
          files: |
            client/manifest.json
            client/main.js
            client/styles.css
            client/checksums.txt
